document.documentElement.classList.add('js');const menu=document.querySelector('.menu'),nav=document.querySelector('#navigation');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));menu.textContent=open?'Close ×':'Menu ☰';nav.classList.toggle('open',open)});nav.addEventListener('click',e=>{if(e.target.closest('a')){menu.setAttribute('aria-expanded','false');menu.textContent='Menu ☰';nav.classList.remove('open')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&nav.classList.contains('open')){menu.click();menu.focus()}});
const content={corp:['One managed workstation. Multiple layers.','PX-WS01 combines domain policy, Windows LAPS and endpoint telemetry. Selected routes reach domain services, file shares and monitoring consoles.','PASSIVE INPUT → SECURITY ONION'],server:['Identity, data and visibility.','PX-DC01 provides AD and DNS; PX-SRV01 hosts departmental files; PX-SEC01 receives Wazuh telemetry; PX-SO01 has its management interface here. Same subnet traffic normally bypasses pfSense.','HOST FIREWALLS + SERVICE PERMISSIONS MATTER'],dmz:['A separate boundary for services.','PX-DMZ01 hosts the lab’s web and mail services. Recorded rules permit selected CORP access and Wazuh transport, with private network blocking after the service exceptions.','SERVICE PERMISSIONS ARE NOT PUBLIC EXPOSURE']};const tabs=[...document.querySelectorAll('[role=tab]')],panel=document.querySelector('#zone-detail');function select(tab){tabs.forEach(t=>{t.setAttribute('aria-selected',String(t===tab));t.tabIndex=t===tab?0:-1});panel.setAttribute('aria-labelledby',tab.id);const [title,body,note]=content[tab.dataset.zone];panel.querySelector('strong').textContent=title;panel.querySelector('p').textContent=body;panel.querySelector('span').textContent=note}tabs.forEach((t,i)=>{t.addEventListener('click',()=>select(t));t.addEventListener('keydown',e=>{let n;if(e.key==='ArrowRight')n=(i+1)%tabs.length;if(e.key==='ArrowLeft')n=(i+tabs.length-1)%tabs.length;if(e.key==='Home')n=0;if(e.key==='End')n=tabs.length-1;if(n!==undefined){e.preventDefault();select(tabs[n]);tabs[n].focus()}})});
document.querySelector('#print-resume')?.addEventListener('click',()=>window.print());const current=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('#navigation a').forEach(a=>{if(a.getAttribute('href')===current)a.setAttribute('aria-current','page')});

const motion=document.createElement('button');motion.className='motion-toggle';motion.textContent='Pause animation';motion.setAttribute('aria-pressed','false');motion.addEventListener('click',()=>{const paused=document.body.classList.toggle('motion-paused');motion.textContent=paused?'Resume animation':'Pause animation';motion.setAttribute('aria-pressed',String(paused))});if(document.querySelector('.hero-art'))document.querySelector('.footer-links')?.append(motion);document.addEventListener('click',e=>{if(nav.classList.contains('open')&&!e.target.closest('.site-header')){menu.click()}});window.matchMedia('(min-width:1151px)').addEventListener('change',e=>{if(e.matches&&nav.classList.contains('open'))menu.click()});
